node {
     stage('env') {
         sh 'whoami'
         sh 'echo $PATH'
         sh 'echo test'
     }
     stage('Clone repository') {
         checkout scm
     }

     stage('Build image') {
         app = docker.build("jenkins-docker-pipeline/my-image:${tag}")
     }
}